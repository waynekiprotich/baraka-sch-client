{
  "name": "baraka-school-kapsabet",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "admin",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin",
    "lint": "npm run lint --prefix frontend && npm run lint --prefix admin",
    "format": "npm run format --prefix frontend && npm run format --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}