// Placeholder for future FastAPI backend integration.
// When the backend is ready, replace mock calls with real fetch requests.
const API_BASE = '/api';

export const adminApi = {
  // login: (email, password) =>
  //   fetch(`${API_BASE}/auth/login`, {
  //     method: 'POST',
  //     headers: { 'Content-Type': 'application/json' },
  //     body: JSON.stringify({ email, password }),
  //   }).then((r) => r.json()),
};

export default adminApi;